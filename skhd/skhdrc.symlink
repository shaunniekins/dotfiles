# ################################################################ #

# App shortcuts 
alt - c : open -a 'Visual Studio Code'
alt - u : open -a 'Google Chrome'

# alt - return : osascript -e 'tell application "Terminal" to create window with default profile command ""' || open -na Terminal
alt - return : open -a 'Terminal'